"""Fallback Engine — domain-agnostic semantic allocation core.

The oracle (schemas + invariants + fixtures) is built before the engine so the
Core Engine can be developed test-first against a green/red signal.
"""

__version__ = "0.1.0"
